.venv/
__pycache__/
*.pyc
.streamlit/secrets.toml
.DS_Store
# Pre-built DB — generated by `python app.py builddb`; baked into Docker image during build
tanach.db

# Exploratory Talmud↔Tanakh gematria research: dataset + scripts + generated
# write-up. Kept locally for possible further work; not part of the deployed app.
talmud-research/
talmud-gematria-findings.html
talmud-gematria-findings.pdf

# Local self-hosting: hidden launcher + watchdog that keep the app running on
# this PC and served over Tailscale Funnel. Machine-specific, not part of the app.
host/
