# Tanach Gematria Engine — runtime dependencies
# Tested on Python 3.12 with the exact versions noted in comments.
# Pinned, not floated: the loader-icon replacement and app-view layout target
# Streamlit-internal test ids (stStatusWidget, stSidebarCollapsedControl) that
# carry no stability promise. An unpinned rebuild has silently broken DOM
# assumptions here before. Re-pin deliberately after verifying live.
streamlit==1.58.0
pandas>=2.1            # tested 3.0.2
numpy>=1.26            # transitive (seaborn/matplotlib); pinned for reproducibility
matplotlib>=3.8        # tested 3.10.8
seaborn>=0.13          # tested 0.13.2
plotly>=5.18           # interactive charts in Tab 4
